{{!--
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link rel="stylesheet" href="styles/edit.css" type="text/css">
</head>

<body>
    <div class="head">
        <form id="edit-form" action="/edit" method="post">
            <input type="hidden" id="id" name="id" value="{{ editdata._id }}">
            <label for="username">Course:</label>
            <input type="text" id="course" name="course" value="{{ editdata.course }}" required><br>

            <label for="password">Duration:</label>
            <input type="text" id="duration" name="duration" value="{{ editdata.duration }}" required><br>

            <label for="description">Description:</label>
            <textarea id="description" type="text" name="description" rows="7" cols="80"
                required>{{ editdata.description }}</textarea><br><br>
            <input type="submit" value="Submit">
            <a href="/course">Create</a>
        </form>
    </div>



    <ul>
        <h1>Course Summary</h1>
        {{#each list}}
        <li>
            <h5>Course</h5>{{this.course}}
            <h5>Duration</h5>{{this.duration}} <br>
            <h5>Description</h5>{{this.description}}<br><br>

            <a href="/edit?edit_id={{this._id}}">Update Your Account</a>
            <a href="/edit?delete_id={{this._id}}"
                onclick="if(!confirm('Are you sure you want to delete?')){ return false; }">Delete Your Account</a>
        </li>
        {{/each}}
    </ul>


</body>

</html>

--}}


<!DOCTYPE html>
<html lang="en">

<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        crossorigin="anonymous">
     <link rel="stylesheet" href="styles/course.css" type="text/css">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet" />
    <!-- MDB -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.2.0/mdb.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/styles/course.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.4/css/dataTables.bootstrap5.min.css">
</head>

<body>
    <a href="/course">Create</a>
    <button type="button" id="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#exampleModal">
        <box-icon name='plus-medical' color='#282a36'></box-icon>
    </button>

    <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="exampleModalLabel">edit Course</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form action="/edit" method="post">
                        <div class="mb-3">
                            <input type="hidden" id="id" name="id" value="{{ editdata._id }}">
                            <label for="username">Course:</label>
                            <input type="text" id="course" name="course" value="{{ editdata.course }}" required><br>
                        </div>
                        <div class="mb-3">
                            <label for="type">Type:</label>
                            <input type="text" id="duration" name="duration" value="{{ editdata.duration }}"
                                required><br>
                        </div>
                        <div class="mb-3">
                            <label for="link">Link To Redirect:</label>
                            <input type="text" id="link" name="link" value="{{ editdata.link }}" required><br>
                        </div>
                        <div class="mb-3">
                            <label for="category">Category:</label>
                            <input type="text" id="link" name="category" value="{{ editdata.category }}" required><br>
                        </div>
                        <div class="dropdown">
                            <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown"
                                aria-expanded="false">
                                Category List
                            </button>
                            <ul class="dropdown-menu">
                                <p>serverside | programming | html and css | javascript (Try to use the same word)</p>
                            </ul>
                        </div>

                        <div class="mb-3">
                            <label for="description" class="form-label">Description</label>
                            <textarea class="form-control" id="description" name="description" rows="3"
                                required>{{ editdata.description }}</textarea><br><br>
                        </div>
                        <input type="submit" value="Submit">
                    </form>
                </div>
            </div>
        </div>
    </div>
 <h1 style="color:rgb(14, 116, 206); margin-top: 8rem;">To Edit and Update</h1>
    <table id="example" class="table table-dark table-striped" style="width:100%;">
        <thead>
            <tr>
                <th>Course</th>
                <th>Type</th>
                <th>Category</th>
                <th>Link</th>
                <th>Description</th>
                <th>Action</th>
                <th>Action</th>

            </tr>
        </thead>
        <tbody>
            {{#each list}}
            <tr>
                <td>{{this.course}}</td>
                <td>{{this.duration}}</td>
                <td>{{this.category}}</td>
                <td>{{this.link}}</td>
                <td>{{this.description}}</td>
                <td> <button type="button" class="btn btn-primary" data-bs-toggle="modal"
                        data-bs-target="#staticBackdrop">
                        <a href="/edit?edit_id={{this._id}}">Update</a>
                    </button></td>
                <td> <button type="button" class="btn btn-primary" data-bs-toggle="modal"
                        data-bs-target="#staticBackdrop">
                        <a href="/edit?delete_id={{this._id}}" data-delete>Delete</a>
                    </button></td>
            </tr>

            {{/each}}

        </tbody>

    </table>


    <!-- Modal -->
    <div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
        aria-labelledby="staticBackdropLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="staticBackdropLabel">Confirm Delete</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <h5>Are you sure want to delete ?</h5>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    <a href="" class="btn btn-danger" id="confirm-delete-btn">Delete</a>
                </div>
            </div>
        </div>
    </div>
    <!-- Bootstrap JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"
        integrity="sha384-zYPOMqeu1DAVkHiLqWBUTcbYfZ8osu1Nd6Z89ify25QV9guujx43ITvfi12/QExE"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.min.js"
        integrity="sha384-Y4oOpwW3duJdCWv5ly8SCFYWqFDsfob/3GkgExXKV4idmbt98QcxXYs9UoXAB7BZ"
        crossorigin="anonymous"></script>
    <script src="https://unpkg.com/boxicons@2.1.4/dist/boxicons.js"></script>
    <!-- JavaScript for delete confirmation -->
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.2.0/mdb.min.js"></script>
    <script defer src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script defer src="https://cdn.datatables.net/1.13.4/js/jquery.dataTables.min.js"></script>
    <script defer src="https://cdn.datatables.net/1.13.4/js/dataTables.bootstrap5.min.js"></script>
    <script defer src="./JS/script.js"></script>
    <!-- JavaScript for delete confirmation -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // Handle delete button click
            var deleteButtons = document.querySelectorAll('a[data-delete]');
            for (var i = 0; i < deleteButtons.length; i++) {
                deleteButtons[i].addEventListener('click', function (event) {
                    event.preventDefault();
                    var url = this.getAttribute('href');

                    // Set the URL for the delete button in the modal
                    document.querySelector('#confirm-delete-btn').setAttribute('href', url);

                    // Show the modal
                    var confirmDeleteModal = document.querySelector('#confirm-delete-modal');
                    confirmDeleteModal.classList.add('show');
                    confirmDeleteModal.style.display = 'block';
                    var modalBackdrop = document.createElement('div');
                    modalBackdrop.classList.add('modal-backdrop', 'fade', 'show');
                    document.body.appendChild(modalBackdrop);
                });
            }
        });


        $('#example').on('click', '.btn-primary', function () {
            // Get the row data from the clicked button
            const $row = $(this).closest('tr');
            const userName = $row.find('td:eq(0)').text();
            const fName = $row.find('td:eq(1)').text();
            const lName = $row.find('td:eq(2)').text();
            const email = $row.find('td:eq(4)').text();
            const psswd = $row.find('td:eq(5)').text();

            // Set the values of the input fields in the modal
            $('#username').val(userName);
            $('#fname').val(fName);
            $('#lname').val(lName);
            $('#email').val(email);
            $('#password').val(psswd);

            // Show the modal
            $('#updateModal').modal('show');
        });



        $(document).ready(function () {
            $('#example').DataTable();
        });

        $(document).ready(function () {
            $('#example').on('click', '.btn-primary', function () {
                $('#updateModal').modal('show');
            });
        });
    </script>


</body>

</html>